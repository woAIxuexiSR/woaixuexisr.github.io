# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

# Hidden system files
*~
.DS_Store
Thumbs.db

# Kiro
.kiro

# Test code — kept local only, never deployed.
# (These are also excluded from the Jekyll build via `exclude:` in _config.yml.)
/spec/
/test/
/.rspec
/vitest.config.js
/package.json
